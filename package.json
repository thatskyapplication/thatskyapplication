{
  "name": "caelus",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm install && npm run-script clean && tsc",
    "clean": "rimraf sourceJS/",
    "prepare": "husky install",
    "start": "npm run-script build && BRANCH=$(git rev-parse --abbrev-ref HEAD) COMMIT_HASH=$(git rev-parse HEAD) ENVIRONMENT=Production node --require dotenv/config sourceJS/Caelus.js",
    "test": "eslint . && tsc"
  },
  "dependencies": {
    "date-fns-tz": "^1.3.4",
    "discord.js": "^14.0.0-dev.1655381041-4df491c",
    "dotenv": "^16.0.1",
    "mariadb": "^3.0.0",
    "node-fetch": "^3.2.5"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.27.0",
    "@typescript-eslint/parser": "^5.27.0",
    "eslint": "^8.17.0",
    "eslint-plugin-import": "^2.26.0",
    "husky": "^8.0.1",
    "lint-staged": "^13.0.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.7.3"
  }
}
