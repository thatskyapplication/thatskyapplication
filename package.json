{
	"name": "caelus",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "npm install && tsc",
		"format": "prettier --write . && eslint --fix .",
		"prepare": "husky install",
		"start": "node --require dotenv/config sourceJS/Caelus.js",
		"test": "prettier --check . && eslint . && tsc"
	},
	"dependencies": {
		"date-fns-tz": "^1.3.7",
		"discord.js": "^14.6.0",
		"dotenv": "^16.0.3",
		"knex": "^2.3.0",
		"pg": "^8.8.0"
	},
	"devDependencies": {
		"@jiralite/eslint-config-neon": "^0.1.36",
		"eslint": "^8.27.0",
		"husky": "^8.0.2",
		"lint-staged": "^13.0.3",
		"prettier": "^2.7.1",
		"typescript": "^4.8.4"
	}
}
