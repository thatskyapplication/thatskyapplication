{
  "name": "caelus",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm install && tsc",
    "prepare": "husky install",
    "start": "BRANCH=$(git rev-parse --abbrev-ref HEAD) COMMIT_HASH=$(git rev-parse HEAD) node --require dotenv/config sourceJS/Caelus.js",
    "test": "eslint . && tsc"
  },
  "dependencies": {
    "date-fns-tz": "^1.3.7",
    "discord.js": "^14.5.0",
    "dotenv": "^16.0.3",
    "mariadb": "^3.0.1",
    "node-fetch": "^3.2.10"
  },
  "devDependencies": {
    "@jiralite/eslint-config-neon": "^0.1.35",
    "eslint": "^8.24.0",
    "husky": "^8.0.1",
    "lint-staged": "^13.0.3",
    "typescript": "^4.8.4"
  }
}
