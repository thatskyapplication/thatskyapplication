import { Area } from "../../models/area.js";
import { AreaName, RealmName } from "../geography.js";

export default new Area({
	name: AreaName.TrialOfWater,
	realm: RealmName.IsleOfDawn,
	wingedLight: 1,
});
